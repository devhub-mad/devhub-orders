apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: devhub-orders-el
  namespace: devhub2
spec:
  serviceAccountName: pipeline
  triggers:
    - bindings:
        - kind: ClusterTriggerBinding
          ref: github-push
      interceptors:
        - params:
            - name: eventTypes
              value: ['push']
          ref:
            name: github
        - params:
            - name: git-revision
              value: >-
                (body.ref.contains('main'))
            - name: overlays
              value: null
          ref:
            name: cel
      template:
        ref: devhub-orders-run-pipeline
